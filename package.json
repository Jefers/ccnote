{
  "name": "cnotes",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A static, installable PWA for fitness coach client notes.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.8",
    "jsdom": "^27.0.1",
    "sharp": "^0.35.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vitest": "^4.1.8"
  }
}
